version: '3.4'

services:
  examplebasics:
    image: examplebasics
    build:
      context: project
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
